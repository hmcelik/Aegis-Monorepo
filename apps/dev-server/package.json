{
  "name": "@telegram-moderator/dev-server",
  "private": true,
  "version": "1.0.0",
  "description": "Development server for serving examples",
  "type": "module",
  "engines": { "node": ">=22.0.0" },
  "packageManager": "pnpm@10.14.0",
  "scripts": {
    "dev": "node src/server.js",
    "start": "node src/server.js",
    "build": "echo \"(js project) no build step\"",
    "test": "echo \"no tests configured\"",
    "lint": "echo \"no linter configured\""
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
